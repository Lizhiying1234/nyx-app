/**
 * 屏上关于**同步**的那几句话 —— 一份，两端共用（D-238 / COPY_RULES CR-7）。
 *
 * ══ 为什么单独一个文件 ════════════════════════════════════════
 * 这几句话的共同点不是「属于同步模块」，而是**同一句话有好几处在说**：
 * 主进程写一次（日志 / 自检报告）、设置页写一次、设计规范页再写一次。
 * 以前它们各写各的字面量，改一处另外两处不会报错（文案审查 R-04 / R-06）。
 * ★ 这个文件**只收「多处在说同一句」的那几句**。只有一处在用的句子留在原地 ——
 *   把所有文案都搬过来会变成一个谁都往里塞的杂物间，那时它就不再守着什么了。
 */

/**
 * 设置里那颗开关的名字。三处在说：
 * 设置页的开关与它的 `aria-label` · 设计规范页那一行 · 主进程那次自动同步的理由。
 */
export const AUTO_SYNC_ON_BOOT = '开机自动同步'

/**
 * 上一次同步留下了没做完的事。两处在说：
 * 自检报告里那一条（`main/db/audit.ts`）· 设置 › 同步那块红框的标题。
 */
export const SYNC_INCOMPLETE_TITLE = '上一次同步没有完全成功'
