/**
 * 判层用的固定基线 · D-467（2026-09-07）
 *
 * ── 它从哪来 ────────────────────────────────────────────────
 *
 * 原来这一行常量长在 `src/main/assess.ts` 里，叫 `FALLBACK_LEVEL`：
 * 「还没评估过之前先给一个保守基线」。而评估那一层（`assessments` /
 * `profile_facts` / 五道冷启动诊断题 / 「我的水平」整块界面）
 * **使用者 2026-09-07 取消了**，`assess.ts` 整个文件删掉 ——
 * 于是这行常量必须活下来，因为它**一直在喂核心判断**：
 *
 *   M-006 的「表达差距法」判的是「一个处在学习者当前水平的人会怎么写这个意思」，
 *   D-177 / M-008 把这句话里的「当前水平」交给了评估。评估没了，
 *   这句话还在 —— `prompts/analyse-item.md` 与 `generate-questions.md`
 *   的 `{{LEVEL}}` 每一次调用都要填。填不上就得瞎编一个，
 *   而**瞎编出来的等级不会报错**，只会让判层悄悄偏一档。
 *
 * ── 为什么放进 core ────────────────────────────────────────
 *
 * D-467 明写「改成常量留在 core，不再有界面」：手机那边（D-R22 单条解析）
 * 装配同一份请求，两端填进 `{{LEVEL}}` 的必须是同一个字符串 ——
 * 各写一份的话，同一条知识点在电脑和手机上会得到按不同水平写的解析，
 * 而两边都说得通、都不报错。
 *
 * ★ 这是一个**固定值**，不是「默认值」：现在没有任何东西会改它。
 *   将来要按真实数据校准（M-008 曾经的做法：静默库攒够 50 条之后
 *   拿他已经会用的表达当基线），那是一条新决议，不是改这里的字面量。
 */
export const LEVEL_BASELINE = 'upper-intermediate (B2)'
