/**
 * 「同一个说法」的那把尺 —— 唯一一份（D-026 · 2026-09-05 由 T-2.11 上提到 core）
 *
 * ── 为什么它必须在 core ──────────────────────────────────────
 *
 * 这是**判据**，不是平台细节：D-026 的「再收一次是信号」用它，
 * `analyze.ts` 跳过已收过的表达用它，T-2.11 的合并判据按它分组。
 * 判据不在平台层（D-238 / D-365）。
 *
 * 它原来住在 `src/main/db/repo.ts:24`。留在那里的代价是具体的：
 * `core/dedup/plan.ts` 要按同一把尺分组，而 core **不许** import main ——
 * 于是只剩两条路，一条是在 core 里再写一份（**两份判据**，这个项目已经
 * 为它付过好几次学费），一条就是把这一份搬上来。搬上来之后
 * `main/db/repo.ts` 原样 re-export，三个调用点一个字都不用改。
 *
 * ★ Android 的 `capture.ts:26` 目前是同源的**第二份拷贝**。本轮不动它
 *   （跨仓改文件不在 T-2.11 范围内）；等下一次提 submodule 指针时
 *   让它改成从 core 读，那时才算真的只剩一份。
 *
 * ── 尺子本身 ────────────────────────────────────────────────
 *
 * 只做四件事：去首尾空白 · 转小写 · 压中间空白 · 去掉行尾标点。
 * **故意不做词形还原** —— `bear the brunt of` 与 `bore the brunt of` 是不是
 * 同一条，使用者可能有不同看法；猜错的后果是「我明明没删过它，它却不进来了」，
 * 那种失败他看不见（`main/db/ledger.ts::normTerm` 头上记着同一条理由）。
 */
export function normalizeTerm(s: string): string {
  return s
    .trim()
    .toLowerCase()
    .replace(/\s+/g, ' ')
    .replace(/[.,;:!?"'’”）)]+$/u, '')
}
